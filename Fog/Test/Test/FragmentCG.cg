struct texture_Output
{
	float3 color:COLOR;
};

void FragmentMain(float2 texCoord:TEXCOORD0, 
					float4 iColor:COLOR,
					uniform sampler2D pic:TEX0,
					out float4 oColor:COLOR)
{
	texture_Output result;
	//oColor = tex2D(pic, texCoord);
	oColor = iColor * tex2D(pic, texCoord);
}